# FullWorth Context Compatibility Pointer

The authoritative project continuation document is now:

`FULLWORTH_CONTEXT.md`

This legacy filename is retained temporarily so older project instructions that still reference `BILLWATCH_CONTEXT.md` fail safely instead of silently losing continuation context.

Do not maintain duplicate project state in this file. Read `FULLWORTH_CONTEXT.md` for the current FullWorth development handoff.
